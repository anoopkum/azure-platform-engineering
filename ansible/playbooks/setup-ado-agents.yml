---
- name: Provision self-hosted Azure DevOps agents
  hosts: ado_agents
  become: true
  # ado_pat must be passed via --extra-vars or as env var ADO_PAT
  # Non-interactive: ansible-playbook ... -e "ado_pat=$ADO_PAT"
  roles:
    - node-baseline
    - ado-agent
